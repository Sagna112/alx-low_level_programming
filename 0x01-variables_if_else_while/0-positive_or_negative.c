#include <stdio.h>

int main(void)
	/**
	 * main -Entry point
	 */
	 int n = 0;
	 /* C condition statement */
	if( n > 0)
{ printf("is positive\n");}
          else if( n == 0) {
	printf("is zero\n");
	/* it should return zero */
	  else {/* it should return its negative */  printf("is negative\n");}
	  return (0);
